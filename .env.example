# Frontend env. Baked at build time (Vite). NEVER put secrets here — bundle is public.

# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:8000

# Build-time only flags
VITE_APP_VERSION=0.1.0
